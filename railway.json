{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": ["haral-shop/**"]
  },
  "deploy": {
    "healthcheckPath": "/ko",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
